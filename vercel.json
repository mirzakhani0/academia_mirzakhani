{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/Spike/browser",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
